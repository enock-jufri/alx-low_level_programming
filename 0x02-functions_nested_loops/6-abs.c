#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * Return: 0
 */
int _abs(int)
{
	if (int < 0)
		putchar('int *= -1');
	else if (int > 0)
		putchar(int);
	else
		putchar('0');
}
